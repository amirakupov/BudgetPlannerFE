import React from 'react';
import { useLocation, Link } from 'react-router-dom';
import { Navbar, Nav, Container } from 'react-bootstrap';

const Navigation = () => {
/*   const location = useLocation();

  // Verstecke die Navbar auf der StartingPage
  if (location.pathname === '/' || location.pathname === '/login' || location.pathname === '/register') {  
    return null;
  }

  return (
    <Navbar bg="light" expand="lg" className="mb-4">
      <Container fluid>
        <Navbar.Brand as={Link} to="/" className="font-weight-bold">
          Budget Planner
        </Navbar.Brand>
        <Navbar.Toggle aria-controls="basic-navbar-nav" />
        <Navbar.Collapse id="basic-navbar-nav">
          <Nav className="ml-auto font-weight-bold">
            <Nav.Link as={Link} to="/" className="nav-link">Home</Nav.Link>
            <Nav.Link as={Link} to="/homepage" className="nav-link">Home</Nav.Link>
            <Nav.Link as={Link} to="/balance" className="nav-link">Balance</Nav.Link>
            <Nav.Link as={Link} to="/income" className="nav-link">Incomes</Nav.Link>
            <Nav.Link as={Link} to="/expense" className="nav-link">Expenses</Nav.Link>
          </Nav>
        </Navbar.Collapse>
      </Container>
    </Navbar>
  ); */
};

export default Navigation;